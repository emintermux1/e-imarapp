# API (Fastify / website BFF)
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# Cesium 3D — optional; without it terrain falls back to ellipsoid (no Ion world terrain)
# Get a token at https://cesium.com/ion/ — never commit real tokens
NEXT_PUBLIC_CESIUM_ION_TOKEN=
