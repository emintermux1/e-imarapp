# Legacy frontend archive

The canonical product frontend is `apps/e_imar_web`.

This archive contains old/reference frontends moved out of active app paths so
agents and humans do not edit the wrong product shell:

- `apps/web`
- `apps/web-next`
- `apps/e_imar_next`
- `frontend`

Treat these as historical reference only. Any code worth keeping should be
migrated into `apps/e_imar_web` deliberately.
